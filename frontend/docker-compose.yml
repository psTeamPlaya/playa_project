services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend.dev
    ports:
      - "5173:5173"
    
    environment:
      - WATCHPACK_POLLING=true 
    stdin_open: true 
    tty: true
